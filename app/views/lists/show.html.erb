<h1><%= @list.name %></h1>
<p>This is list show-- should show contents within a particular lists</p>

<ul>
<% @list.companies.each do |company| %>
    <li><%= link_to company.title, company %></li>
<% end %>
</ul>

<p>This link will only edit the list NAME - Need to find a way to edit the contents/company names within the list</p>
<%= link_to 'edit', edit_list_path(@list) %>

<%= link_to 'Destroy',  @list,  method: :delete, data: { confirm: 'Are you sure?' } %>